dist: trusty
sudo: false
language: bash
addons:
  apt:
    packages:
    - python-pip

script:
- pip install --user gh-pr-comment
- sleep 20
- echo "test file" > /tmp/test.txt
- gh-pr-comment "Bot comment" "Test comment body."
